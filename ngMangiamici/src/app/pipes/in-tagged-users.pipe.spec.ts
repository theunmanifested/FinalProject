import { InTaggedUsersPipe } from './in-tagged-users.pipe';

describe('InTaggedUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new InTaggedUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
